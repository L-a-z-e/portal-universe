# ===============================================
# Portal Shell - Kubernetes Environment
# (Kubernetes 클러스터 환경용)
# ===============================================

# 빌드 프로파일 지정
VITE_PROFILE=k8s

# API Gateway 주소 (Ingress를 통해 접근)
VITE_API_BASE_URL=http://portal-universe:8080/api

# Micro-Frontend Remote URL (Ingress를 통해 접근)
VITE_BLOG_REMOTE_URL=http://portal-universe:8080/blog/assets/remoteEntry.js
VITE_SHOP_REMOTE_URL=http://portal-universe:8080/shop/assets/remoteEntry.js

# ===============================================
# OIDC (OpenID Connect) 설정
# ===============================================
# 인증 서버(Auth Service) 주소 (Ingress 경유)
VITE_OIDC_AUTHORITY=http://portal-universe:8080/auth-service
# 클라이언트 ID
VITE_OIDC_CLIENT_ID=portal-client
# 로그인 후 리다이렉트될 URI (Ingress 경유)
VITE_OIDC_REDIRECT_URI=http://portal-universe:8080/callback
# 로그아웃 후 리다이렉트될 URI (Ingress 경유)
VITE_OIDC_POST_LOGOUT_REDIRECT_URI=http://portal-universe:8080
# 인증 방식 (Authorization Code Flow)
VITE_OIDC_RESPONSE_TYPE=code
# 요청할 권한 범위
VITE_OIDC_SCOPE='openid profile read write'
