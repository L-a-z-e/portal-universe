export interface BadgeProps {
  /**
   * 색상 변형
   */
  variant?: 'default' | 'primary' | 'success' | 'warning' | 'danger' | 'info' | 'outline';

  /**
   * 크기
   */
  size?: 'xs' | 'sm' | 'md' | 'lg';
}