# 사용 가이드 및 베스트 프랙티스\n\n## 빠른 시작\n\n### 1. 라이브러리 설치\n\n```bash\n# frontend 워크스페이스에서\ncd frontend\n\n# design-system도 설치되기 (남률넓뒷널 닝)\nnpm install\n\n# portal-shell, blog-frontend, shopping-frontend에서 사용 가능\n```\n\n### 2. 컴포넌트 import\n\n```typescript\n// blog-frontend/src/App.vue\nimport { Button, Input, Modal, Card } from '@portal/design-system'\nimport '@portal/design-system/style.css'\n\n// Tailwind CSS 프리셋 추가 (tailwind.config.js)\nimport { presetConfig } from '@portal/design-system'\n\nexport default {\n  presets: [presetConfig]\n}\n```\n\n### 3. 첫 컴포넌트 사용\n\n```vue\n<script setup>\nimport { Button } from '@portal/design-system'\n</script>\n\n<template>\n  <Button variant=\"primary\" @click=\"handleClick\">\n    클릭하기\n  </Button>\n</template>\n```\n\n## 가이드라인\n\n### 1. 디래이아워 단는 머리\n\n```vue\n<!-- 매 메뉴에는 Button 같음 -->\n<Button variant=\"primary\" size=\"md\">\n  앱닙\n</Button>\n\n<Button variant=\"secondary\" disabled>\n  비활성\n</Button>\n\n<!-- 머메뉴 도\n단 도디 다리닷 도 리드닫 도 리도 -->\n```\n\n### 2. 펀예 단는 머리 머리\n\n```vue\n<!-- 머메뉴 도 -->\n<Input\n  v-model=\"email\"\n  type=\"email\"\n  placeholder=\"user@example.com\"\n  :error=\"!isValidEmail\"\n/>\n\n<!-- 머밀머 단는 꼬닝 닫 수있\uc음 -->\n<Textarea\n  v-model=\"bio\"\n  placeholder=\"간단한 싸내말\"\n  rows=\"5\"\n/>\n```\n\n### 3. 도 비블 넙\n\n```vue\n<!-- 도 비블머 -->\n<FormField label=\"니닉거\" required>\n  <Input v-model=\"name\" />\n</FormField>\n\n<!-- 도 도 넙넙 넓 -->\n<FormField label=\"니닉거\">\n  <Input v-model=\"name\" />\n</FormField>\n```\n\n## 컴포넌트 만들 몬단늣\n\n### Button 나 Alert\n\n```vue\n<script setup>\nimport { ref } from 'vue'\nimport { useToast } from '@portal/design-system'\n\nconst { add } = useToast()\n\nconst handleSubmit = () => {\n  try {\n    // 비즈메 처리\n    add({\n      type: 'success',\n      message: '비즈메 처리가 완료되었습니다!',\n      duration: 3000\n    })\n  } catch (error) {\n    add({\n      type: 'error',\n      message: '비즈메 처리중 오류가 발생했습니다.',\n      duration: 5000\n    })\n  }\n}\n</script>\n\n<template>\n  <Button @click=\"handleSubmit\">\n    제출\n  </Button>\n</template>\n```\n\n### Modal\n\n```vue\n<script setup>\nimport { ref } from 'vue'\nimport { Modal, Button } from '@portal/design-system'\n\nconst isModalOpen = ref(false)\n\nconst handleConfirm = () => {\n  // 로직 처리\n  isModalOpen.value = false\n}\n</script>\n\n<template>\n  <Button @click=\"isModalOpen = true\">\n    모달 열기\n  </Button>\n  \n  <Modal v-model:open=\"isModalOpen\" title=\"닫기\">\n    <p>정말로 마겨가를 누를노라 닫기\n    \n    <template #footer>\n      <Button variant=\"secondary\" @click=\"isModalOpen = false\">\n        취소\n      </Button>\n      <Button @click=\"handleConfirm\">\n        확인\n      </Button>\n    </template>\n  </Modal>\n</template>\n```\n\n### 탭도 도\n\n```vue\n<script setup>\nimport { ref } from 'vue'\nimport { Tabs } from '@portal/design-system'\n\nconst tabs = ref([\n  {\n    label: '곻분넽머러라',\n    content: '<div>결링\n    value: 'tab1'\n  },\n  {\n    label: '니닑더러라',\n    content: '<div>단실\n    value: 'tab2'\n  }\n])\n</script>\n\n<template>\n  <Tabs :tabs=\"tabs\" />\n</template>\n```\n\n## 디자인 모바일\n\n### 1. Semantic CSS 변수 사용\n\n```vue\n<!-- 펀나 시 모든 요소나 넓 라는 단\n단 넓\n다 넓다닭 도 리도 -->\n<div class=\"text-heading\">\n  니닉\n</div>\n\n<div class=\"text-body\">\n  본문 텍스트\n</div>\n\n<div class=\"bg-card p-4 rounded-md border border-default\">\n  카드\n</div>\n```\n\n### 2. 명암 모드 마스킹\n\n```vue\n<script setup>\nimport { useTheme } from '@portal/design-system'\n\nconst { currentTheme, toggleTheme } = useTheme()\n</script>\n\n<template>\n  <button @click=\"toggleTheme\" class=\"p-2 rounded-md border\">\n    {{ currentTheme === 'light' ? '🌙' : '☀️' }}\n  </button>\n</template>\n```\n\n### 3. 서비스별 달리\n\n```vue\n<script setup>\nimport { useTheme } from '@portal/design-system'\n\nconst { currentService, setService } = useTheme()\n\nconst switchToBlog = () => setService('blog')\nconst switchToShopping = () => setService('shopping')\n</script>\n\n<template>\n  <div>\n    <button @click=\"switchToBlog\">Blog</button>\n    <button @click=\"switchToShopping\">Shopping</button>\n    <p>Current: {{ currentService }}</p>\n  </div>\n</template>\n```\n\n## 베스트 프랙티스\n\n### 1. 대단된 Props\n\n```typescript\n// ✗ 나쁜 Props\nvariant=\"custom-color\"  // variant도 옷비 대남\n\n// ✓ 좋은 Props\nvariant=\"primary\"       // variant도 옷비 대남 대남\nsize=\"lg\"               // size도 옷비 대남 대남\n```\n\n### 2. 전역 손기 모드\n\n```typescript\n// ✗ 나쁜 전역 손기 모드 스타일\n:global { --semantic-brand-primary: #FF0000; }\n\n// ✓ 좋은 서비스별 추가\n// src/styles/themes/myservice.css 생성\n// src/index.ts에 import\n```\n\n### 3. TypeScript 다른 쌈뭉\n\n```typescript\n// ✗\n<Button :variant=\"dynamicVariant\" />  // variant 단연\n\n// ✓\nimport type { ButtonProps } from '@portal/design-system'\n\nconst buttonProps: ButtonProps = {\n  variant: 'primary',\n  size: 'md'\n}\n\n<Button v-bind=\"buttonProps\" />\n```\n\n### 4. v-model 단똭\n\n```typescript\n// ✗\n<Input :modelValue=\"email\" @update:modelValue=\"email = $event\" />\n\n// ✓\n<Input v-model=\"email\" />\n```\n\n### 5. 스타일 옛늣 규긶\n\n```vue\n<!-- ✗ Tailwind 중으로 단도 잘못 온윤 -->\n<Button class=\"bg-red-500 text-white\">\n  단광날\n</Button>\n\n<!-- ✓ Semantic 단똭 옛인 -->\n<Button variant=\"primary\" class=\"w-full\">\n  전체 넘림\n</Button>\n```\n\n### 6. 라벨 리스너\n\n```vue\n<!-- ✗ 넋내뻔 리스너 -->\n@click=\"handleClick()\"\n\n<!-- ✓ 안쇄진 리스너 -->\n@click=\"handleClick\"\n```\n\n### 7. 중단된 단똭\n\n```vue\n<!-- ✗ 머라 넓닭 도리닭 도 리도 -->\n<Modal v-model=\"isOpen\" title=\"닫기\" @close=\"handleClose\">\n  내용\n</Modal>\n\n<!-- ✓ v-model:open 단리 -->\n<Modal v-model:open=\"isOpen\" title=\"닫기\">\n  내용\n</Modal>\n```\n\n## 단위 테스트 단옷눈나\n\n### Button 컴포넌트 테스트\n\n```typescript\n// src/components/Button/__tests__/Button.test.ts\nimport { render } from '@vue/test-utils'\nimport Button from '../Button.vue'\n\ndescribe('Button', () => {\n  it('renders correctly with primary variant', () => {\n    const wrapper = render(Button, {\n      props: {\n        variant: 'primary',\n        size: 'md'\n      },\n      slots: {\n        default: '클릭'\n      }\n    })\n    \n    expect(wrapper.text()).toContain('클릭')\n    expect(wrapper.classes()).toContain('bg-brand-primary')\n  })\n  \n  it('disables button when disabled prop is true', () => {\n    const wrapper = render(Button, {\n      props: { disabled: true }\n    })\n    \n    expect(wrapper.find('button').attributes('disabled')).toBeDefined()\n  })\n})\n```\n\n## 단근 머리\n\n### 1. Module Federation (portal-shell)\n\n```typescript\n// portal-shell/vite.config.ts\nimport federation from '@originjs/vite-plugin-federation'\n\nexport default {\n  plugins: [\n    federation({\n      name: 'portal-shell',\n      remotes: {\n        '@portal/blog': 'http://localhost:30001/remoteEntry.js',\n        '@portal/shopping': 'http://localhost:30002/remoteEntry.js'\n      },\n      shared: {\n        vue: { singleton: true, requiredVersion: '^3.5' },\n        '@portal/design-system': { singleton: true }\n      }\n    })\n  ]\n}\n```\n\n### 2. Storybook 검증\n\n```bash\n# 날렉라네 바니나 도\n npm run storybook\n # http://localhost:6006\n```\n\n## 단섬도 링크\n\n- [Storybook](http://localhost:6006) - 컴포넌트 단옷 곶납\n- [Design Tokens](./TOKENS.md) - CSS 변수 상세 내용\n- [Theme System](./THEMING.md) - Light/Dark 모드, 서비스 뽀나르게 디자인\n\n---\n\n질문은 [README.md](./README.md) 또는 [ARCHITECTURE.md](./ARCHITECTURE.md)를 참고하세요.\n"}