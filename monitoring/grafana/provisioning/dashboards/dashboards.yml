apiVersion: 1

providers:
  - name: 'Portal Universe'
    orgId: 1
    folder: 'Portal Universe'
    folderUid: 'portal-universe'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
