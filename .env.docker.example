# ===================================================================
# Portal Universe - Docker Environment Variables (Infrastructure Only)
# ===================================================================
# Copy this file to .env.docker and fill in the values
# cp .env.docker.example .env.docker
#
# NOTE: Service-specific secrets (OAuth, encryption keys) belong in
# each service's own .env.docker file (e.g., services/auth-service/.env.docker)
# ===================================================================

# --- Database (MySQL) ---
MYSQL_ROOT_PASSWORD=your-secure-root-password
MYSQL_USER=laze
MYSQL_PASSWORD=your-secure-password

# --- Database (MongoDB) ---
MONGO_INITDB_ROOT_USERNAME=laze
MONGO_INITDB_ROOT_PASSWORD=your-secure-password

# --- Database (PostgreSQL for Prism) ---
POSTGRES_USER=laze
POSTGRES_PASSWORD=your-secure-password
POSTGRES_DB=prism

# --- Grafana ---
GF_SECURITY_ADMIN_USER=admin
GF_SECURITY_ADMIN_PASSWORD=your-secure-password
