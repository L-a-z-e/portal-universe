server:
  port: 8888

spring:
  application:
    name: config-service

  cloud:
    config:
      server:
        native:
          search-locations: ${CONFIG_REPO_LOCATION:file:./config-repo/}

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka