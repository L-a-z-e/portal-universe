spring:
  config:
    import:
  cloud:
    config:
      enabled: false
    discovery:
      enabled: false
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://dummy-issuer-for-test

feign:
  auth-service:
    url: http://localhost:8081

aws:
  s3:
    access-key: test-access-key
    secret-key: test-secret-key
    region: ap-northeast-2
    endpoint: http://localhost:4566
    bucket-name: test-bucket
