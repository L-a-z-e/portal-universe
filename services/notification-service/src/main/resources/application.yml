server:
  port: 8084

spring:
  application:
    name: notification-service
  config:
    import: "configserver:${CONFIG_SERVER_URL:http://config-service:8888}"
  cloud:
    config:
      fail-fast: true