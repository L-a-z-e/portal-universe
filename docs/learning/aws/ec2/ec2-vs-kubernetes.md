# EC2 vs 컨테이너 서비스 비교

## 학습 목표
- EC2 직접 관리와 컨테이너 오케스트레이션의 차이 이해
- ECS, EKS, Fargate 등 AWS 컨테이너 서비스 비교
- Kubernetes가 마이크로서비스에 적합한 이유 파악
- Portal Universe가 Kubernetes를 선택한 배경 이해
- 각 서비스의 적합한 사용 사례 학습

---

## 1. 컴퓨팅 서비스 진화

### 1.1 컴퓨팅 모델의 변천

```
[1세대: 물리 서버]
┌─────────────────────┐
│   Physical Server   │
│  ┌───────────────┐  │
│  │ Application   │  │
│  │ OS            │  │
│  │ Hardware      │  │
│  └───────────────┘  │
└─────────────────────┘
- 고정 용량
- 긴 프로비저닝 시간
- 낮은 활용률

          ↓

[2세대: 가상 머신 (EC2)]
┌─────────────────────┐
│   Virtual Machine   │
│  ┌─────┐  ┌─────┐  │
│  │ App │  │ App │  │
│  │ OS  │  │ OS  │  │
│  └─────┘  └─────┘  │
│   Hypervisor        │
│   Hardware          │
└─────────────────────┘
- 탄력적 용량
- 빠른 프로비저닝
- 높은 활용률

          ↓

[3세대: 컨테이너 (Docker)]
┌─────────────────────┐
│   Container Host    │
│  ┌─────┐  ┌─────┐  │
│  │ App │  │ App │  │
│  │ Libs│  │ Libs│  │
│  └─────┘  └─────┘  │
│   Container Engine  │
│   Host OS           │
│   Hardware          │
└─────────────────────┘
- 경량화 (초 단위 시작)
- 일관성 (어디서나 동일)
- 높은 밀도

          ↓

[4세대: 오케스트레이션 (Kubernetes)]
┌──────────────────────────────────┐
│      Kubernetes Cluster          │
│  ┌──────┐ ┌──────┐ ┌──────┐     │
│  │ Pod1 │ │ Pod2 │ │ Pod3 │     │
│  └──────┘ └──────┘ └──────┘     │
│  자동 스케일링, 로드 밸런싱       │
│  자가 치유, 롤링 업데이트         │
└──────────────────────────────────┘
- 자동화된 배포
- 선언적 구성
- 서비스 디스커버리
```

---

## 2. EC2 직접 관리 vs 컨테이너

### 2.1 배포 프로세스 비교

```
[EC2 직접 관리]
┌────────────────────────────────────────┐
│ 1. EC2 인스턴스 생성                    │
│ 2. SSH 접속                            │
│ 3. 의존성 설치 (Java, Node.js 등)     │
│ 4. 애플리케이션 배포                    │
│ 5. 수동 설정 (환경 변수, 방화벽 등)    │
│ 6. 프로세스 관리 (systemd, supervisord)│
└────────────────────────────────────────┘
문제점:
- 환경 불일치 (로컬 vs 프로덕션)
- 수동 작업 많음
- 스케일링 복잡
- 롤백 어려움

          vs

[컨테이너 (Kubernetes)]
┌────────────────────────────────────────┐
│ 1. 컨테이너 이미지 빌드                 │
│ 2. kubectl apply -f deployment.yaml    │
│ 3. 자동 배포 완료 ✅                    │
└────────────────────────────────────────┘
장점:
- 환경 일관성 (이미지에 모든 것 포함)
- 선언적 배포 (YAML 정의)
- 자동 스케일링
- 원클릭 롤백
```

### 2.2 스케일링 비교

**EC2 직접 관리:**
```bash
# 수동 스케일 아웃
1. 새 EC2 인스턴스 생성
2. 로드 밸런서에 수동 등록
3. 애플리케이션 배포
4. 헬스 체크 확인
5. 트래픽 분산 확인

⏱️ 소요 시간: 5-10분
🔧 수동 개입: 필수
```

**Kubernetes:**
```yaml
# 자동 스케일링 정의
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: blog-service
spec:
  minReplicas: 2
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
```
```
⏱️ 소요 시간: 30초~1분
🔧 수동 개입: 불필요
📊 자동 모니터링 및 조정
```

---

## 3. AWS 컨테이너 서비스 비교

### 3.1 서비스 개요

| 서비스 | 타입 | 관리 범위 | 벤더 종속성 |
|--------|------|-----------|-------------|
| **EC2** | IaaS | 서버 직접 관리 | 낮음 |
| **ECS** | CaaS | AWS 네이티브 오케스트레이션 | 높음 |
| **EKS** | CaaS | 관리형 Kubernetes | 낮음 |
| **Fargate** | Serverless | 서버리스 컨테이너 | 중간 |

### 3.2 ECS (Elastic Container Service)

**AWS 네이티브 컨테이너 오케스트레이션 서비스**

```
┌──────────────────────────────────────────┐
│            ECS Cluster                    │
├──────────────────────────────────────────┤
│  ┌────────────────┐  ┌────────────────┐ │
│  │ EC2 인스턴스 1 │  │ EC2 인스턴스 2 │ │
│  │  ┌──────────┐  │  │  ┌──────────┐  │ │
│  │  │ Task 1   │  │  │  │ Task 3   │  │ │
│  │  │ Task 2   │  │  │  │ Task 4   │  │ │
│  │  └──────────┘  │  │  └──────────┘  │ │
│  └────────────────┘  └────────────────┘ │
└──────────────────────────────────────────┘
```

**특징:**
```
✅ 장점:
- AWS와 긴밀한 통합 (ALB, CloudWatch, IAM)
- 학습 곡선 낮음
- AWS 서비스와 자동 연동

❌ 단점:
- AWS 종속적 (멀티 클라우드 불가)
- 커뮤니티가 Kubernetes보다 작음
- Kubernetes 생태계 도구 사용 불가
```

**사용 사례:**
- AWS만 사용하는 환경
- 간단한 컨테이너 워크로드
- AWS 서비스와의 통합이 중요한 경우

### 3.3 EKS (Elastic Kubernetes Service)

**AWS에서 제공하는 관리형 Kubernetes**

```
┌──────────────────────────────────────────────┐
│         EKS Control Plane (AWS 관리)          │
│  - API Server                                │
│  - etcd                                      │
│  - Controller Manager                        │
│  - Scheduler                                 │
└──────────────┬───────────────────────────────┘
               │
┌──────────────┴───────────────────────────────┐
│         Worker Nodes (사용자 관리)            │
│  ┌────────────────┐  ┌────────────────┐     │
│  │ EC2 노드 1     │  │ EC2 노드 2     │     │
│  │  ┌──────┐     │  │  ┌──────┐     │     │
│  │  │ Pod1 │     │  │  │ Pod3 │     │     │
│  │  │ Pod2 │     │  │  │ Pod4 │     │     │
│  │  └──────┘     │  │  └──────┘     │     │
│  └────────────────┘  └────────────────┘     │
└──────────────────────────────────────────────┘
```

**특징:**
```
✅ 장점:
- 표준 Kubernetes (벤더 독립적)
- 거대한 커뮤니티와 생태계
- 멀티 클라우드 마이그레이션 가능
- Helm, Istio 등 도구 활용

➖ 단점:
- 학습 곡선 높음
- 관리 복잡도 증가
- 비용이 ECS보다 높을 수 있음
```

**사용 사례:**
- 마이크로서비스 아키텍처
- 멀티 클라우드 전략
- 표준 Kubernetes 생태계 활용
- 복잡한 오케스트레이션 요구사항

### 3.4 Fargate

**서버리스 컨테이너 실행 환경**

```
┌──────────────────────────────────────────┐
│         ECS/EKS + Fargate                │
├──────────────────────────────────────────┤
│  사용자는 컨테이너만 정의                 │
│  AWS가 인프라 자동 관리                   │
│                                          │
│  ┌──────┐  ┌──────┐  ┌──────┐          │
│  │ Task │  │ Task │  │ Task │          │
│  └──────┘  └──────┘  └──────┘          │
│                                          │
│  EC2 인스턴스 관리 불필요 ✅              │
└──────────────────────────────────────────┘
```

**특징:**
```
✅ 장점:
- 서버 관리 불필요 (No EC2)
- 자동 스케일링
- 사용한 만큼만 과금 (vCPU/메모리 단위)
- 보안 강화 (컨테이너 격리)

❌ 단점:
- 비용이 EC2보다 높음 (20-30%)
- 제한된 커스터마이징
- 콜드 스타트 지연 가능
```

**사용 사례:**
- 배치 작업
- 가변적인 워크로드
- 인프라 관리 부담 최소화

---

## 4. 비교표

### 4.1 기능 비교

| 기능 | EC2 직접 | ECS | EKS | Fargate |
|------|---------|-----|-----|---------|
| **서버 관리** | 전체 관리 필요 | EC2 관리 필요 | 노드 관리 필요 | 불필요 ✅ |
| **컨테이너 오케스트레이션** | 직접 구현 | AWS 제공 | Kubernetes | AWS 제공 |
| **학습 난이도** | 낮음 | 중간 | 높음 | 중간 |
| **자동 스케일링** | 수동/ASG | 자동 | 자동 | 자동 |
| **멀티 클라우드** | 가능 | 불가 | 가능 ✅ | 불가 |
| **커뮤니티 생태계** | 제한적 | AWS 중심 | 매우 큼 ✅ | AWS 중심 |
| **서비스 디스커버리** | 직접 구현 | Cloud Map | 내장 ✅ | Cloud Map |
| **헬스 체크** | 직접 구현 | 내장 | 내장 ✅ | 내장 |
| **롤링 업데이트** | 직접 구현 | 지원 | 지원 ✅ | 지원 |

### 4.2 비용 비교 (예시: 2 vCPU, 4GB RAM, 24시간 실행)

```
[EC2 (t3.medium)]
인스턴스: $0.0416/hour × 24 = $0.9984/day
스토리지 (30GB): $3/month
총 비용: ~$33/month

[ECS + EC2]
인스턴스: $0.0416/hour × 24 = $0.9984/day
ECS: 무료
총 비용: ~$30/month

[EKS + EC2]
Control Plane: $0.10/hour × 24 = $2.4/day (고정)
Worker Node: $0.0416/hour × 24 = $0.9984/day
총 비용: ~$102/month
⚠️ Control Plane 비용이 추가됨

[Fargate]
vCPU: $0.04048/hour × 2 × 24 = $1.943/day
메모리: $0.004445/GB/hour × 4 × 24 = $0.427/day
총 비용: ~$71/month
```

**비용 결론:**
```
단일 서비스: EC2/ECS가 저렴
다수 서비스 (5개 이상): EKS가 효율적
가변 워크로드: Fargate 고려
```

---

## 5. Portal Universe가 Kubernetes를 선택한 이유

### 5.1 마이크로서비스 아키텍처

Portal Universe는 **폴리글랏 마이크로서비스**를 지향합니다.

```
┌──────────────────────────────────────────────────┐
│          Portal Universe Services                 │
├──────────────────────────────────────────────────┤
│  auth-service (Java/Spring Boot)                 │
│  blog-service (Java/Spring Boot)                 │
│  shopping-service (Java/Spring Boot)             │
│  notification-service (Java/Spring Boot)         │
│  [향후] analytics-service (Go)                   │
│  [향후] recommendation-service (Python)          │
└──────────────────────────────────────────────────┘
```

**Kubernetes의 이점:**
- **언어 독립적:** Java, Go, Python 모두 컨테이너로 동일하게 관리
- **독립 배포:** 각 서비스를 별도로 배포 및 스케일링
- **서비스 메시:** Istio, Linkerd 등 고급 기능 추가 가능

### 5.2 로컬 개발 환경

```
┌──────────────────────────────────────────────┐
│   개발자 노트북 (macOS/Linux/Windows)         │
├──────────────────────────────────────────────┤
│  Kind (Kubernetes in Docker)                 │
│  ┌────────────────────────────────────────┐  │
│  │  Local Kubernetes Cluster              │  │
│  │  ┌────┐ ┌────┐ ┌────┐ ┌────┐          │  │
│  │  │Auth│ │Blog│ │Shop│ │Ntfy│          │  │
│  │  └────┘ └────┘ └────┘ └────┘          │  │
│  │  ┌──────────────────────────────────┐  │  │
│  │  │ Kafka, Redis, PostgreSQL, MySQL  │  │  │
│  │  └──────────────────────────────────┘  │  │
│  └────────────────────────────────────────┘  │
└──────────────────────────────────────────────┘

💡 프로덕션과 동일한 환경을 로컬에서 재현!
```

**EC2로는 불가능:**
- 로컬에서 EC2를 에뮬레이션할 수 없음
- LocalStack은 제한적
- Docker Compose는 단순하지만 오케스트레이션 부족

### 5.3 클라우드 벤더 독립성

```
┌────────────────────────────────────────┐
│     Kubernetes YAML                    │
│  (클라우드 독립적 정의)                 │
└───────────┬────────────────────────────┘
            │
    ┌───────┼───────┐
    │       │       │
    ▼       ▼       ▼
┌─────┐ ┌─────┐ ┌─────┐
│ AWS │ │ GCP │ │Azure│
│ EKS │ │ GKE │ │ AKS │
└─────┘ └─────┘ └─────┘
   또는 온프레미스 Kubernetes
```

**장점:**
- AWS 종속 위험 감소
- 멀티 클라우드 전략 가능
- 온프레미스 마이그레이션 용이

### 5.4 자동화 및 GitOps

```
┌──────────────────────────────────────────┐
│         Git Repository                    │
│  k8s/                                    │
│  ├─ auth-service/deployment.yaml         │
│  ├─ blog-service/deployment.yaml         │
│  └─ shopping-service/deployment.yaml     │
└──────────────┬───────────────────────────┘
               │
               ▼
┌──────────────────────────────────────────┐
│      ArgoCD / Flux (GitOps)              │
│  - Git 변경 감지                          │
│  - 자동 배포                              │
│  - 롤백 자동화                            │
└──────────────┬───────────────────────────┘
               │
               ▼
┌──────────────────────────────────────────┐
│      Kubernetes Cluster                  │
└──────────────────────────────────────────┘
```

**EC2 직접 관리로는 어려움:**
- Ansible, Chef 등 별도 도구 필요
- 복잡한 스크립트 관리
- 상태 드리프트 문제

---

## 6. 언제 EC2를 직접 사용해야 하나?

### 6.1 EC2가 적합한 경우

```
✅ 다음 경우에는 EC2 직접 사용 고려:

1. 레거시 애플리케이션
   - 컨테이너화 어려운 구형 앱
   - 특정 OS 버전/패치 필요
   - 라이선스 제약 (CPU/코어 기반)

2. 특수 하드웨어 요구사항
   - GPU 인스턴스 (머신러닝)
   - 고성능 네트워크 (HPC)
   - 특정 CPU 아키텍처

3. 단일 모놀리식 애플리케이션
   - 분산 시스템 아님
   - 수평 확장 불필요
   - 오케스트레이션 오버헤드 불필요

4. 상태 유지 애플리케이션
   - 데이터베이스 서버 (직접 설치)
   - 파일 서버
   - 캐시 서버 (로컬 디스크 중요)

5. 비용 최적화
   - 예약 인스턴스로 장기 할인
   - 매우 작은 규모 (1-2개 서버)
```

### 6.2 Kubernetes가 과도한 경우

```
❌ 오버엔지니어링 주의:

- 단순 웹사이트 (WordPress 등)
  → EC2 1대 또는 Lightsail

- 개인 프로젝트 / 프로토타입
  → EC2 + Docker Compose

- 팀이 Kubernetes 경험 없음
  → ECS로 시작 (학습 곡선 낮음)

- 매우 제한된 예산
  → EC2 Spot 인스턴스
```

---

## 7. 마이그레이션 경로

### 7.1 EC2 → Kubernetes 전환

```
[단계별 마이그레이션]

Step 1: 컨테이너화
┌──────────────────┐       ┌──────────────────┐
│  EC2 인스턴스     │  -->  │  Docker Image    │
│  - Java App      │       │  - Dockerfile    │
│  - systemd       │       │  - 의존성 포함    │
└──────────────────┘       └──────────────────┘

Step 2: 로컬 테스트
┌──────────────────────────────────────┐
│  Docker Compose                      │
│  - 여러 서비스 통합 테스트            │
│  - 네트워크 연결 검증                │
└──────────────────────────────────────┘

Step 3: Kubernetes 배포
┌──────────────────────────────────────┐
│  Kubernetes Deployment               │
│  - YAML 정의                         │
│  - ConfigMap, Secret 분리            │
│  - Service, Ingress 설정             │
└──────────────────────────────────────┘

Step 4: 점진적 전환
┌─────────────┐        ┌─────────────┐
│ EC2 (기존)  │  -->   │ Kubernetes  │
│ 90% 트래픽  │        │ 10% 트래픽  │
└─────────────┘        └─────────────┘
     ↓
┌─────────────┐        ┌─────────────┐
│ EC2 (기존)  │  -->   │ Kubernetes  │
│ 50% 트래픽  │        │ 50% 트래픽  │
└─────────────┘        └─────────────┘
     ↓
┌─────────────┐
│ Kubernetes  │
│ 100% 트래픽 │
└─────────────┘
```

---

## 8. 핵심 정리

### 8.1 선택 가이드

| 요구사항 | 추천 서비스 | 이유 |
|----------|-------------|------|
| 마이크로서비스 아키텍처 | **EKS** | 자동화, 스케일링, 서비스 디스커버리 |
| AWS 전용, 간단한 컨테이너 | **ECS** | 쉬운 통합, 낮은 학습 곡선 |
| 인프라 관리 싫음 | **Fargate** | 서버리스, 자동 스케일링 |
| 레거시 앱, 특수 하드웨어 | **EC2** | 유연성, 완전한 제어 |
| 개발/테스트 환경 | **Kind/Docker Compose** | 로컬 실행, 빠른 반복 |

### 8.2 비용 vs 관리 복잡도

```
           복잡도 높음
                ▲
                │
        EKS     │     Fargate
                │
        ────────┼────────────
                │
        ECS     │     EC2
                │
           복잡도 낮음
        ◄───────┼───────────►
         비용 낮음    비용 높음
```

### 8.3 Portal Universe 선택 요약

```
✅ Kubernetes (EKS) 선택 이유:

1. 폴리글랏 마이크로서비스
2. Kind로 로컬 개발 환경 동일성
3. 클라우드 벤더 독립성
4. GitOps 워크플로우
5. 자동 스케일링 및 자가 치유
6. 거대한 커뮤니티 생태계

📌 초기 학습 비용은 높지만, 장기적으로 생산성 향상
```

---

## 다음 학습

- [Kubernetes 기초](../../kubernetes/kubernetes-introduction.md)
- [EKS 소개](../eks/eks-introduction.md)
- [Docker 컨테이너화](../../docker/docker-introduction.md)
- [GitOps와 ArgoCD](../../cicd/gitops-argocd.md)

---

## 참고 자료

- [AWS 컨테이너 서비스 비교](https://aws.amazon.com/containers/)
- [ECS vs EKS 공식 가이드](https://docs.aws.amazon.com/whitepapers/latest/overview-deployment-options/amazon-ecs-vs-amazon-eks.html)
- [Kubernetes 공식 문서](https://kubernetes.io/docs/)
- [Kind (Kubernetes in Docker)](https://kind.sigs.k8s.io/)
