# AWS 개요

## 학습 목표

- 클라우드 컴퓨팅의 개념과 장점 이해
- AWS 핵심 서비스 카테고리 파악
- Portal Universe에서 사용하는 AWS 서비스 확인
- On-Premise vs Cloud 차이점 인식

---

## 1. 클라우드 컴퓨팅이란?

클라우드 컴퓨팅은 **인터넷을 통해 IT 리소스를 온디맨드로 제공**하고 사용한 만큼만 비용을 지불하는 모델입니다.

### On-Premise vs Cloud

```
[전통적 방식: On-Premise]
┌─────────────────────────────────────┐
│ 1. 서버 구매 (수백만~수천만원)      │
│ 2. 데이터센터 구축/임대            │
│ 3. 네트워크 설정                   │
│ 4. 유지보수 인력 채용              │
│ 5. 전력/냉각 비용                  │
└─────────────────────────────────────┘
⏱️  준비 기간: 수주~수개월
💰 초기 투자: 매우 큼


[클라우드 방식]
┌─────────────────────────────────────┐
│ 1. AWS 계정 생성 (무료)             │
│ 2. 필요한 서비스 선택 및 실행       │
│ 3. 사용한 만큼 비용 지불            │
└─────────────────────────────────────┘
⏱️  준비 기간: 몇 분
💰 초기 투자: 없음 (종량제)
```

### 클라우드 컴퓨팅의 장점

| 장점 | 설명 | 예시 |
|------|------|------|
| **유연성** | 필요할 때 즉시 리소스 확보 | 트래픽 급증 시 서버 추가 |
| **비용 효율** | 사용한 만큼만 지불 | 야간 시간대 서버 축소 |
| **글로벌 확장** | 전 세계 어디서나 배포 | 아시아/유럽 동시 서비스 |
| **고가용성** | 장애 대응 자동화 | 한 서버 장애 시 자동 전환 |
| **보안** | 엔터프라이즈급 보안 | DDoS 방어, 암호화 |

---

## 2. AWS(Amazon Web Services)란?

Amazon이 제공하는 **클라우드 컴퓨팅 플랫폼**으로, 2006년 출시 이후 글로벌 1위 클라우드 서비스입니다.

### AWS 시장 점유율 (2024년 기준)

```
AWS:          32% ██████████████████████████████████
Azure:        23% ███████████████████████████
Google Cloud: 11% ███████████████
기타:         34% ██████████████████████████████████
```

### 핵심 특징

- **200개 이상의 서비스** 제공
- **전 세계 33개 리전** (서울 포함)
- **수백만 고객** 사용 (Netflix, Airbnb, Samsung 등)
- **Pay-as-you-go** 요금제

---

## 3. AWS 핵심 서비스 카테고리

### 3.1 Compute (컴퓨팅)

코드를 실행하는 서비스입니다.

| 서비스 | 설명 | 사용 사례 |
|--------|------|-----------|
| **EC2** | 가상 서버 | 웹 서버, 애플리케이션 서버 |
| **Lambda** | 서버리스 함수 | 이벤트 기반 처리 |
| **ECS/EKS** | 컨테이너 오케스트레이션 | Docker/Kubernetes 앱 |

```
Portal Universe (현재)
├── Local: Docker Compose
└── Production (향후): EKS (Kubernetes)
```

### 3.2 Storage (스토리지)

데이터를 저장하는 서비스입니다.

| 서비스 | 설명 | 사용 사례 |
|--------|------|-----------|
| **S3** | 객체 스토리지 | 파일, 이미지, 백업 |
| **EBS** | 블록 스토리지 | EC2 디스크 볼륨 |
| **EFS** | 파일 시스템 | 공유 파일 시스템 |

```
Portal Universe (현재) ✓
└── S3: Blog Service 파일 업로드
    └── Bucket: portal-blog-files
```

### 3.3 Database (데이터베이스)

관리형 데이터베이스 서비스입니다.

| 서비스 | 유형 | 사용 사례 |
|--------|------|-----------|
| **RDS** | 관계형 DB (MySQL, PostgreSQL) | 트랜잭션 데이터 |
| **DynamoDB** | NoSQL | 키-값, 문서 저장소 |
| **ElastiCache** | 인메모리 캐시 (Redis) | 세션, 캐시 |

```
Portal Universe (현재)
├── MySQL: Auth, Blog, Shopping
├── MongoDB: Blog (게시글)
└── Redis: 캐시, 세션 (로컬)
```

### 3.4 Networking (네트워킹)

네트워크 구성 서비스입니다.

| 서비스 | 설명 | 사용 사례 |
|--------|------|-----------|
| **VPC** | 가상 네트워크 | 격리된 네트워크 환경 |
| **CloudFront** | CDN | 정적 콘텐츠 배포 |
| **Route 53** | DNS | 도메인 관리 |
| **ELB** | 로드 밸런서 | 트래픽 분산 |

### 3.5 Messaging (메시징)

비동기 통신 서비스입니다.

| 서비스 | 설명 | 사용 사례 |
|--------|------|-----------|
| **SQS** | 메시지 큐 | 비동기 작업 처리 |
| **SNS** | Pub/Sub | 알림, 이벤트 브로드캐스트 |
| **EventBridge** | 이벤트 버스 | 서비스 간 이벤트 라우팅 |

```
Portal Universe (현재)
└── Kafka: 이벤트 기반 통신
    (향후 SQS/SNS 추가 고려)
```

### 3.6 Monitoring & Logging (모니터링)

시스템 관찰 서비스입니다.

| 서비스 | 설명 | 사용 사례 |
|--------|------|-----------|
| **CloudWatch** | 메트릭/로그 | CPU, 메모리 모니터링 |
| **X-Ray** | 분산 추적 | 마이크로서비스 디버깅 |

```
Portal Universe (현재)
├── Prometheus: 메트릭 수집
├── Grafana: 대시보드
├── Loki: 로그 집계
└── Zipkin: 분산 추적
```

---

## 4. 가장 많이 사용되는 AWS 서비스 Top 10

```
순위  서비스        사용률   주요 용도
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 1    EC2          ████████ 가상 서버
 2    S3           ████████ 파일 저장
 3    RDS          ███████  관계형 DB
 4    Lambda       ██████   서버리스
 5    CloudFront   ██████   CDN
 6    VPC          ██████   네트워크
 7    IAM          ██████   권한 관리
 8    CloudWatch   █████    모니터링
 9    ELB          █████    로드 밸런서
10    SQS          ████     메시지 큐
```

---

## 5. Portal Universe에서의 AWS 활용

### 현재 사용 중 ✓

```
┌─────────────────────────────────────────┐
│         Blog Service                    │
│                                         │
│  ┌──────────┐         ┌──────────┐     │
│  │Spring Boot│────────►│LocalStack│     │
│  │  App     │  Upload │   S3     │     │
│  └──────────┘         └──────────┘     │
│                             │           │
│                      파일 저장소        │
└─────────────────────────────────────────┘
```

**사용 서비스:**
- **S3**: Blog 첨부 파일 저장
- **LocalStack**: 로컬 개발 환경에서 S3 에뮬레이션

### 향후 확장 계획 (예시)

```
Portal Universe + AWS
├── Compute
│   └── EKS: Kubernetes 클러스터 (마이크로서비스 배포)
├── Storage
│   ├── S3: 정적 파일 (이미지, 첨부파일)
│   └── EFS: 공유 파일 시스템
├── Database
│   ├── RDS: MySQL (Auth, Blog, Shopping)
│   └── ElastiCache: Redis (세션, 캐시)
├── Messaging
│   └── MSK: Managed Kafka (이벤트 스트리밍)
├── Networking
│   ├── CloudFront: CDN (Vue/React 앱)
│   └── ALB: Application Load Balancer
└── Monitoring
    ├── CloudWatch: 로그/메트릭
    └── X-Ray: 분산 추적
```

---

## 6. AWS vs 다른 클라우드 비교

| 항목 | AWS | Azure | Google Cloud |
|------|-----|-------|--------------|
| **출시년도** | 2006 | 2010 | 2011 |
| **시장 점유율** | 1위 (32%) | 2위 (23%) | 3위 (11%) |
| **서비스 수** | 200+ | 200+ | 100+ |
| **강점** | 생태계, 성숙도 | MS 통합 | AI/ML, BigQuery |
| **한국 리전** | ✓ (서울) | ✓ (서울) | ✓ (서울) |

### 선택 기준

```
AWS 선택 이유:
✓ 가장 많은 서비스와 기능
✓ 풍부한 레퍼런스와 커뮤니티
✓ LocalStack으로 로컬 개발 가능
✓ 한국 기업 다수 사용 (인력 확보 용이)
```

---

## 7. Portal Universe 적용

### 로컬 개발: LocalStack

```bash
# LocalStack 시작 (AWS 서비스 에뮬레이션)
cd docker-localstack
docker-compose up -d

# S3 버킷 생성
awslocal s3 mb s3://portal-blog-files

# 파일 업로드 테스트
echo "Test file" > test.txt
awslocal s3 cp test.txt s3://portal-blog-files/

# 버킷 내용 확인
awslocal s3 ls s3://portal-blog-files/
```

### Blog Service S3 통합

```java
// services/blog-service/src/.../config/S3Config.java
@Configuration
public class S3Config {

    @Bean
    public S3Client s3Client() {
        return S3Client.builder()
            .endpointOverride(URI.create("http://localhost:4566")) // LocalStack
            .region(Region.AP_NORTHEAST_2)
            .build();
    }
}

// services/blog-service/src/.../service/FileService.java
@Service
public class FileService {

    private final S3Client s3Client;

    public String uploadFile(MultipartFile file) {
        PutObjectRequest request = PutObjectRequest.builder()
            .bucket("portal-blog-files")
            .key(file.getOriginalFilename())
            .build();

        s3Client.putObject(request,
            RequestBody.fromBytes(file.getBytes()));

        return "s3://portal-blog-files/" + file.getOriginalFilename();
    }
}
```

---

## 8. 실습 예제

### 실습 1: LocalStack으로 AWS 체험

```bash
# 1. LocalStack 상태 확인
docker-compose ps

# 2. S3 버킷 생성
awslocal s3 mb s3://my-first-bucket

# 3. 버킷 목록 확인
awslocal s3 ls
# 2024-01-22 10:30:00 my-first-bucket

# 4. 파일 업로드
echo "Hello AWS!" > hello.txt
awslocal s3 cp hello.txt s3://my-first-bucket/

# 5. 파일 다운로드
awslocal s3 cp s3://my-first-bucket/hello.txt downloaded.txt
cat downloaded.txt
# Hello AWS!

# 6. 버킷 삭제
awslocal s3 rb s3://my-first-bucket --force
```

### 실습 2: Blog Service S3 업로드 테스트

```bash
# 1. Blog Service 실행 확인
curl http://localhost:8081/actuator/health

# 2. 파일 업로드 API 호출
curl -X POST http://localhost:8081/api/v1/files/upload \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -F "file=@test-image.png"

# 응답 예시:
# {
#   "success": true,
#   "data": {
#     "fileUrl": "s3://portal-blog-files/test-image.png",
#     "fileName": "test-image.png",
#     "fileSize": 102400
#   }
# }

# 3. LocalStack S3에서 확인
awslocal s3 ls s3://portal-blog-files/
```

---

## 9. 핵심 정리

| 개념 | 설명 |
|------|------|
| **클라우드 컴퓨팅** | 인터넷으로 IT 리소스를 온디맨드 제공 |
| **AWS** | 가장 큰 클라우드 플랫폼 (200+ 서비스) |
| **주요 카테고리** | Compute, Storage, Database, Networking, Messaging |
| **Portal Universe** | S3를 Blog 파일 저장소로 사용 |
| **LocalStack** | 로컬에서 AWS 서비스 에뮬레이션 |

### 꼭 기억할 서비스

```
필수:
- S3: 파일 저장소 (Portal Universe 사용 중)
- EC2: 가상 서버
- RDS: 관계형 데이터베이스

권장:
- Lambda: 서버리스 함수
- CloudFront: CDN
- IAM: 권한 관리
```

---

## 10. 다음 학습

- [리전과 가용영역](./region-az.md) - AWS 글로벌 인프라 이해
- [AWS CLI 설정](./aws-cli-setup.md) - CLI 도구 설치 및 사용
- [IAM 기초](../iam/iam-fundamentals.md) - 권한 관리의 핵심
- [S3 기초](../s3/s3-fundamentals.md) - 객체 스토리지 심화

---

## 참고 자료

- [AWS 공식 문서](https://docs.aws.amazon.com/)
- [AWS 10분 튜토리얼](https://aws.amazon.com/getting-started/hands-on/)
- [LocalStack 문서](https://docs.localstack.cloud/)
- [AWS 한국 블로그](https://aws.amazon.com/ko/blogs/korea/)
- [Portal Universe - LocalStack S3](../../infra/localstack-s3.md)
